import React from 'react';
import {Text, View} from "react-native";

const Te = () => {
    return (
        <View className="flex  p-10 bg-black">
            <Text className="text-red-500">Try editing me! 🎉</Text>
        </View>
    );
};

export default Te;